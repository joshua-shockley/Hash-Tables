# if collision occurs then need to create another node with that node.next being previous LL head

# delete will also need to ustilize the LL if the index/hashed key exists but isnt the head of the LL
# will need to change the pointers
